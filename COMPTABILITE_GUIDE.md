# دليل تطبيق المحاسبة (`comptabilite`)

## 1. الغرض ونطاق العمل

تطبيق `comptabilite` في GestiFlow هو سجل نقدي مبسّط لتسجيل **الإيرادات** و**المصروفات** يدوياً ومتابعة إجماليهما. وهو ليس نظام محاسبة عامة كاملاً: لا توجد قيود مزدوجة، ولا دفتر أستاذ أو حسابات محاسبية، ولا ربط تلقائي بين العمليات والفواتير أو المشتريات.

العملة المعروضة في الواجهة هي الدينار الجزائري (`DA`). التطبيق مثبت في إعدادات Django ضمن `apps.comptabilite`، وتُركَّب مساراته تحت البادئة `/comptabilite/`.

## 2. الملفات والمسؤوليات

| الملف | المسؤولية |
| --- | --- |
| `apps/comptabilite/models.py` | تعريف كيانَي التصنيف والحركة المالية. |
| `apps/comptabilite/forms.py` | تعريف نموذج إدخال الحركة وتنسيق حقوله بـ Bootstrap. |
| `apps/comptabilite/views.py` | عرض السجل، التصفية، التجميع، وترحيل إضافة حركة. |
| `apps/comptabilite/urls.py` | مسارا السجل وإنشاء الحركة. |
| `apps/comptabilite/admin.py` | إتاحة الحركات والتصنيفات في Django Admin. |
| `templates/comptabilite/historique.html` | شاشة السجل وملخّص الإيرادات/المصروفات. |
| `templates/comptabilite/form.html` | شاشة **إضافة حركة جديدة** المشار إليها في الطلب. |
| `apps/comptabilite/migrations/0001_initial.py` | إنشاء جداول التطبيق الأولية. |

## 3. نموذج البيانات

### 3.1 `CategorieDepense` — تصنيف المصروف

يمثل قائمة التصنيفات المتاحة لاختيارها داخل الحركة.

| الحقل | النوع | القاعدة | المعنى |
| --- | --- | --- | --- |
| `id` | مفتاح أساسي | يُنشأ تلقائياً | المعرّف الداخلي. |
| `nom` | `CharField(100)` | فريد وإلزامي | اسم التصنيف، مثل إيجار أو نقل. |
| `cree_le` | تاريخ ووقت | تلقائي عند الإنشاء | وقت إنشاء التصنيف. |
| `modifie_le` | تاريخ ووقت | يُحدَّث تلقائياً | وقت آخر تعديل. |

التصنيفات مرتبة أبجدياً حسب `nom`. إنشاء التصنيفات أو تعديلها لا يتم من واجهة المحاسبة الحالية؛ يتم من لوحة الإدارة Django Admin.

### 3.2 `Transaction` — الحركة المالية

كل صف يمثل عملية مالية واحدة.

| الحقل | النوع والقيم | الإلزام | الوصف |
| --- | --- | --- | --- |
| `id` | مفتاح أساسي | تلقائي | معرّف الحركة. |
| `type_transaction` | `recette` أو `depense` | نعم | إيراد أو مصروف. |
| `montant` | `Decimal(14, 2)` | نعم | المبلغ بدقة منزلتين عشريتين، حتى 12 رقماً قبل الفاصلة. |
| `libelle` | نص حتى 200 حرف | نعم | الوصف أو البيان. |
| `date_transaction` | تاريخ | نعم | تاريخ العملية؛ افتراضياً تاريخ اليوم. |
| `mode_reglement` | `especes` / `virement` / `cheque` | نعم | نقداً / تحويل / شيك. الافتراضي نقداً. |
| `categorie` | مفتاح أجنبي اختياري إلى `CategorieDepense` | لا | تصنيف العملية. |
| `reference` | نص حتى 100 حرف | لا | رقم أو مرجع خارجي. |
| `valide` | منطقي | تلقائي | يحدد ما إذا كانت الحركة تدخل السجل والتقارير؛ افتراضياً `True`. |
| `cree_par` | مفتاح أجنبي اختياري إلى المستخدم | تلقائي في الواجهة | المستخدم الذي أنشأ الحركة. |
| `cree_le` / `modifie_le` | تاريخ ووقت | تلقائي | آثار الإنشاء والتعديل الموروثة من `TimestampedModel`. |

عند حذف تصنيف أو مستخدم، تبقى الحركة محفوظة، وتصبح قيمة الرابط المقابل فارغة (`SET_NULL`). ترتيب الحركات الافتراضي هو الأحدث حسب `date_transaction` ثم `cree_le`.

> ملاحظة: الاسم `CategorieDepense` يوحي بأنها للمصروفات، لكن النموذج لا يمنع اختيارها أيضاً عند تسجيل إيراد.

## 4. المسارات والتدفق

| المسار | الاسم الداخلي | الطريقة | النتيجة |
| --- | --- | --- | --- |
| `/comptabilite/` | `comptabilite:historique` | `GET` | السجل، الإجماليات، والبحث/التصفية الخلفية. |
| `/comptabilite/nouvelle/` | `comptabilite:ajouter` | `GET` و`POST` | عرض نموذج إضافة حركة أو حفظه. |

كلا المسارين محميان بـ `login_required`: غير المسجّل يُحوَّل إلى صفحة الاتصال التي يحددها المشروع. لا توجد حالياً صلاحية منفصلة تمنع أدواراً مثل البائع أو التقني من الوصول أو الإضافة؛ كل مستخدم مصادق عليه يستطيع ذلك.

### سيناريو إضافة حركة

1. يفتح المستخدم صفحة السجل ويختار **Nouvelle transaction**.
2. يعرض الخادم `TransactionForm`، مع تعبئة التاريخ بتاريخ اليوم.
3. يرسل المتصفح النموذج عبر `POST`، مع رمز CSRF.
4. عند نجاح تحقق Django، تُنشأ الحركة ويُسند `cree_par` إلى المستخدم الحالي.
5. تظهر رسالة نجاح ثم يعاد توجيه المستخدم إلى السجل.
6. عند وجود خطأ، يعرض القالب رسالة الخطأ تحت الحقل نفسه مع إبقاء المدخلات.

زر **Annuler** لا يحذف شيئاً؛ يعود فقط إلى السجل.

## 5. نموذج `form.html` بالتفصيل

القالب يرث `templates/base.html`، ولذلك يستخدم التنسيق العام والقائمة الجانبية وBootstrap 5. عنوان المتصفح يساوي المتغير `titre`، الذي تضبطه الواجهة حالياً إلى `Nouvelle transaction`.

بنية النموذج هي:

- `method="post"` بدون رفع ملفات.
- `{% csrf_token %}` لحماية طلبات الإرسال من CSRF.
- حلقة `{% for field in form %}` تعرض الحقول السبعة بالترتيب المحدد في `TransactionForm`.
- شبكة Bootstrap: كل حقل بعرض كامل على الهاتف وبنصف العرض بدءاً من الشاشات المتوسطة (`col-12 col-md-6`).
- تسمية الحقل ثم عنصر الإدخال، وتحتها الأخطاء الخاصة به بلون أحمر.
- زران: حفظ وإلغاء.

الحقول التي تظهر للمستخدم:

1. **Type transaction**: قائمة اختيار بين `Recette` و`Depense`.
2. **Montant**: حقل رقمي بخطوة `0.01`.
3. **Libelle**: حقل نصي للوصف.
4. **Date transaction**: حقل تاريخ HTML5؛ يملأ بتاريخ اليوم عند أول عرض.
5. **Mode reglement**: قائمة وسيلة الدفع.
6. **Categorie**: قائمة التصنيفات المعرفة مسبقاً، ويمكن تركها فارغة.
7. **Reference**: مرجع اختياري.

الحقول لا تتضمن في القالب سمة `required` مكتوبة يدوياً، لكن Django يضيفها للحقول المطلوبة وفق نموذج البيانات. كما أن `valide` و`cree_par` وحقول التواريخ التقنية لا تظهر في النموذج ولا يستطيع المستخدم ضبطها من هذه الشاشة.

## 6. شاشة السجل والإجماليات

شاشة `historique.html` تعرض الحركات ذات `valide=True` فقط، مع جلب التصنيف وصاحب الإنشاء بكفاءة عبر `select_related`.

تتضمن:

- بطاقة **Recettes**: مجموع مبالغ الحركات ذات النوع `recette` ضمن مجموعة النتائج الحالية.
- بطاقة **Dépenses**: مجموع مبالغ الحركات ذات النوع `depense` ضمن مجموعة النتائج الحالية.
- جدولاً يحتوي: التاريخ، البيان، التصنيف، وسيلة الدفع، المبلغ، والنوع.
- تلوين المبلغ والشارة بالأخضر للإيراد وبالأحمر للمصروف.
- علامة ناقص مرئية قبل مبلغ المصروف؛ القيمة المخزنة نفسها ليست محولة تلقائياً إلى سالبة.
- رسالة «Aucune transaction» عندما لا توجد نتائج.

الخادم يدعم معاملات الاستعلام التالية:

| المعامل | المثال | الأثر |
| --- | --- | --- |
| `q` | `/comptabilite/?q=CHEQUE-12` | يبحث في `libelle` أو `reference` بعد تنقية بسيطة للمحارف. |
| `type` | `/comptabilite/?type=depense` | يرشح وفق قيمة نوع الحركة. |
| `page` | `/comptabilite/?page=2` | يعرض صفحة من 30 حركة. |

تُحسب الإجماليات بعد تطبيق البحث وفلتر النوع، أي إنها إجماليات النتائج المرشحة لا إجماليات كل السجل. القالب الحالي لا يعرض حقول بحث أو تصفية ولا روابط انتقال الصفحات، رغم أن المنطق الخلفي يدعمها.

## 7. التكامل مع التقارير

تقرير الربح والخسارة في `apps/rapports/views.py` يستخدم الحركات الصحيحة (`valide=True`) من نوع `depense` خلال المدة المختارة.

المعادلة المستخدمة حالياً هي:

```text
المارجن الإجمالي = مبيعات الفواتير قبل الضريبة − مشتريات الموردين قبل الضريبة
النتيجة الصافية = المارجن الإجمالي − مصروفات comptabilite
```

كما يجمع التقرير المصروفات حسب `categorie__nom`. لا تدخل حركات `recette` المسجلة هنا في هذا التقرير؛ إيراداته تأتي من فواتير المبيعات. ولا يحدث إنشاء تلقائي لحركات محاسبية عند دفع فاتورة أو إنشاء شراء، لذا يجب تجنب التسجيل المزدوج يدوياً عند استخدام هذه البيانات مع التقرير.

## 8. الإدارة والبيانات التشغيلية

تم تسجيل `Transaction` و`CategorieDepense` في Django Admin بالتسجيل الافتراضي. يمكن للمستخدم ذي صلاحيات الإدارة هناك:

- إنشاء التصنيفات التي تظهر في قائمة النموذج.
- تعديل الحركات أو تغيير `valide` لإخفائها من السجل والتقرير.
- إدارة الحركات التي أنشأها المستخدمون.

لا توجد في واجهة التطبيق العادية عمليات لتعديل أو حذف أو إبطال حركة، ولا شاشة لإدارة التصنيفات.

## 9. قواعد وقيود مهمة

- لا توجد قاعدة تحقق صريحة تفرض أن `montant` موجب؛ حقل `DecimalField` يضمن صيغة الرقم وسعته ودقته فقط. ينبغي إدخال المبلغ موجباً والاعتماد على `type_transaction` لتحديد الاتجاه.
- لا يوجد تحقق من أن التصنيف يخص المصروفات فقط.
- لا يُنظَّف `libelle` أو `reference` عبر دوال التنقية المخصصة عند الحفظ؛ Django يحمي العرض الافتراضي في القوالب من HTML غير الموثوق، لكن قواعد تنسيق المراجع ليست مفروضة هنا.
- لا يوجد رقم فريد للحركة ولا مرفق إثبات ولا حقل للطرف المقابل أو حساب بنكي/صندوق.
- الحركات غير الصالحة (`valide=False`) لا تظهر في السجل ولا تدخل حساب مصروفات تقرير الربح والخسارة.
- تاريخ الحركة يمكن أن يكون في الماضي أو المستقبل؛ لا توجد قيود زمنية حالياً.

## 10. تحسينات مقترحة

1. إضافة التحكم بالصلاحيات، مثل قصر الإنشاء والتعديل والإبطال على المحاسب والمدير.
2. التحقق من أن المبلغ أكبر من صفر، ومن ملاءمة التصنيف للمصروف.
3. إضافة واجهة بحث/فلترة وفهرسة صفحات حقيقية في `historique.html`، لأن الخلفية تدعمها الآن فقط.
4. إضافة تعديل وحذف منطقي/إبطال مع سجل تدقيق، بدلاً من الاعتماد على Django Admin.
5. إدخال فلاتر فترة زمنية، ورصيد صافٍ (`recettes - depenses`)، وتصدير CSV/PDF.
6. ربط الحركات اختيارياً بالفواتير والمدفوعات والمشتريات، مع سياسة واضحة لمنع التكرار.
7. تحسين صفحة الإدارة بعرض أعمدة، فلاتر، وبحث، وإدارة تصنيفات من واجهة التطبيق.
8. إضافة اختبارات للنموذج، الحفظ، صلاحيات الأدوار، التصفية، وحساب الإجماليات والتقرير.

## 11. نقاط دخول سريعة للمطور

- [نموذج البيانات](apps/comptabilite/models.py)
- [النموذج](apps/comptabilite/forms.py)
- [الواجهات](apps/comptabilite/views.py)
- [قالب الإضافة](templates/comptabilite/form.html)
- [قالب السجل](templates/comptabilite/historique.html)
- [استهلاك البيانات في تقرير الربح والخسارة](apps/rapports/views.py)
